
import 'react';

// eslint-disable-next-line react/prop-types
const SelectedRegion = ({ region }) => {
    return (
        <div>
            <h3>Вибраний регіон: {region}</h3>
        </div>
    );
};

export default SelectedRegion;
